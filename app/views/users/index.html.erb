<h2>All Users</h2>

<ul>
	<%if @user %>
		<%@user.each do |user|%>
		<li><%=user.name%> </li>
		<%end%>
</ul>
	<%else%>
	<p> No Users Found </p>
	<%end%>


<%=link_to "Add a new User", new_user_url%>
